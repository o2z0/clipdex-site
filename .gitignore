# このリポジトリは PUBLIC で、GitHub Pages の公開元でもある。
# 「コミットした瞬間に公開される」前提で、除外は実際に発生するものだけを挙げる
# (投機的なエントリは足さない — 何を無視しているか分からなくなるため)。

# ブレスト / 設計検討の作業ファイル。
# ワイヤー・内部批評・レビューのやり取りが入るので、公開リポジトリに載せない。
# 検討の成果は ClipDex 側の docs/superpowers/specs/ に spec として残す。
.superpowers/

# macOS が勝手に作るもの
.DS_Store
._*

# Codex (agmsg) がプロジェクトごとに書くフック設定。ローカル絶対パスが入る。
.codex/
